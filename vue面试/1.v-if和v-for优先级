v-if和v-for哪个优先级更高？如果两个同时出现，应该怎么优化得到更好的性能？
源码中找答案compiler/codegen/index.js
源码在执行时先判断是否为for再判断是否为if

v-for优先级高于v-if，源码中先判断for再判断if
如果同时出现，每次渲染都会先执行循环再判断条件，无论如何循环都不可避免，浪费了性能
可以外层进行v-if判断，内部进行v-for循环
如果属性自身带有条件，不得不在循环内部判断，可以使用computed计算属性进行过滤