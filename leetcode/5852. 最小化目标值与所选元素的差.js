/**
 * @param {number[][]} mat
 * @param {number} target
 * @return {number}
 */
var minimizeTheDifference = function(mat, target) {
    for(let i = 0; i < mat.length; i++) {

        for(let j = 0; j < mat[0].length; j++) {
            for(let k = 0; k < mat[0].length; K++){

            }
            sum = sum + mat[i][j];

        }
    }
};
var doSome = function(i,j,mat) {
if(i < mat[0].length){}
}